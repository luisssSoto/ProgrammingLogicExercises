"""Capacity vs Length"""

from whatIsAnArray import cardboard_box1
import accessingElementsInArray

print(f'carboard box 1 capacity: {len(cardboard_box1.show_collection())} and length: {cardboard_box1.get_length()}')